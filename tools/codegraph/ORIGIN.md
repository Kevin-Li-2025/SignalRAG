# Origin

This subproject was migrated from
[`Kevin-Li-2025/CodeGraph`](https://github.com/Kevin-Li-2025/CodeGraph).

It remains isolated under `tools/codegraph/` because it is a codebase-structure
retrieval prototype rather than a runtime dependency of the main SignalRAG web
search application.
