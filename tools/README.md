# Tools

Auxiliary retrieval tools that support SignalRAG experiments without becoming
part of the main `fast_rag` application package.

- [`codegraph/`](codegraph/) is the migrated CodeGraph project for deterministic
  code-structure GraphRAG over AST, import, call, dependency, and hybrid code
  retrieval evidence.
