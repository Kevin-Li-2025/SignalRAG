# Benchmark Results

Checked-in JSON snapshots from SignalRAG web-search benchmark runs.

These files document the main app's retrieval and answer-quality experiments.
They are separate from migrated research subproject artifacts under `evals/`.
